# Compatibility baseline only. Do not copy these binaries into this workspace.
# Official binary baselines installed locally under runtime/ (not committed).
YASA_ENGINE=runtime/yasa-engine-v0.3.2/yasa-engine-linux-x64
UAST4GO_LEGACY=runtime/uast-v0.2.18/uast4go-linux-amd64
UAST4PY_LEGACY=runtime/uast-v0.2.18/uast4py-linux-amd64

# xAST is cloned on demand by Engine prepare scripts and remains untracked.
XAST_ROOT=benchmarks
