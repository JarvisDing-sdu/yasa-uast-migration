# Fixtures

Add focused Go and Python source files here with their legacy-parser UAST JSON outputs.

Each fixture should isolate one syntax or location behavior and name the legacy binary used to generate its expected JSON.
